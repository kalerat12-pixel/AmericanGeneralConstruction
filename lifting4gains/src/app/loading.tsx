export default function Loading() {
  return (
    <div className="shell flex min-h-[60svh] items-center py-24" role="status" aria-live="polite">
      <p className="type-eyebrow">Loading…</p>
    </div>
  );
}
